# Erdos-128 pytest config. Its presence makes this directory the pytest "rootdir",
# so the project is self-contained and does NOT inherit any config from a parent
# folder it happens to live inside.
[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v
